# 有据 · 提审前全链路检测报告

**日期**：2026-09-14
**场景**：上线前检查（数据清空 + 接口全链路自检 + 真机 UI 冒烟 + 提审配置与合规核对）
**执行方式**：主理人编排执行（工具链自检 + 真机自动化；未启用专家团队）

---

## 📌 TL;DR

- 整体结论：🟢 **条件通过** —— 接口层 0 失败、真机 UI 无崩溃、提审配置与合规入口齐全
- 阻塞项：**2 项 P0**（均为运营动作，非代码问题）：生产库测试数据未清、生产内容需预置
- 本次唯一"疑似 bug"（分享链接 404）经查证为**正常行为**（链接帖子不存在时才 404）
- 下一步：服务器执行清空 → 预置内容 → release 包构建 → 提交 AGC

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟡 **条件 Go**（清数据 + 预置内容后即 🟢） |
| 严重度分布 | 🔴 0 / 🟠 0 / 🟡 3 / 🟢 90+ 项通过 |
| 接口层全链路 | **85 / 87 通过，0 失败，2 项需人工确认** |
| 真机 UI 冒烟 | **9 / 9 关键页面通过**，hilog 无 crash、faultlog 为空 |
| 提审配置 | 通过（release 正式证书 / versionCode 1000000 / compatible 6.0.0(20)） |
| 合规入口 | 通过（隐私政策、用户协议、ICP 备案号可点击跳转工信部） |
| 关键行动项 | **5 条**（2 条 P0 为提审前必做） |

---

## 1. 各检测模块核心结论

### 🗄️ 数据清空
- 本地库：`wipe-posts.sql` 执行完成，**8 张关联表计数全为 0**（Post/Comment/Up/Bookmark/CommentUp/PostEvent/DebateVote/Notification(postId)）；清空前已备份至 `/tmp/youju-backup-20260914-011000.sql`
- 生产库：**未执行**（我无法 SSH 到 114.132.102.189，需你在服务器运行）。

### 🔌 接口层全链路（85/87 通过 · 0 失败）
覆盖 13 个阶段：登录/会话 → 发帖与读取（含 5 类参数校验 + 敏感词拦截）→ 互动（点赞幂等/取消、收藏/收藏夹、评论/回复/评论赞）→ 关注与关注流 → 私信（发送/会话/未读/已读）→ 通知（列表/未读数/全部已读）→ 搜索与标签 → 收藏夹 CRUD → 隐私安全（隐私/通知偏好/拉黑/不喜欢/导出/绑定）→ **越权防护**（删改他人帖、删他人评论均被拒）→ 帖生命周期（回收站/恢复/彻底删除/举报/辩论投票）→ 埋点与推送 → 注销（旧 token 立即 401）。

### 📱 真机 UI 冒烟（9/9 通过 · 无崩溃）
冷启动 → 首页推荐流（含 API 发的测试帖正确展示）→ 圈子页 → 发布模式面板 → 消息页 → 我的页（资料/关注粉丝获赞/草稿箱/废纸篓/我的帖子）→ **设置页全子项**（账号与安全/账号绑定/拉黑管理/数据导出/隐私设置/通知设置/通用/深色模式/减少动态效果/清除缓存/关于）→ 帖子详情页（互动栏 4 键、评论区数据一致）→ 底部互动栏渲染正常。
`hilog` 无 jscrash/cppcrash/FATAL，`/data/log/faultlog/faultlogger/` 为空。

### ⚙️ 提审配置与合规
- product 三套配置正确：`default`(debug签名/6.0.0(20))、`debug`(6.1.1(24))、**`release`（正式证书 youju-release-key4-20260815 / 6.0.0(20)）**
- versionCode `1000000`、versionName `1.0.0`
- 源码形态为 **release**（`BUILD_MODE_NAME='release'`、`DEBUG=false`）→ dev 登录桩在发布包自动隐藏
- 合规入口：隐私政策页、用户协议页、ICP 备案号（陕ICP备2026014636号-2A，点击跳工信部）均就位
- `backend/scripts/check-release-config.mjs` → **passed**

---

## 2. 发现的问题与风险（按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 |
|---|--------|------|------|---------|------|
| 1 | 🟡 P0 | 数据 | 生产库 114.132.102.189 | 生产库测试数据未清空，审核员将看到"全链路自检·图文帖""被提及测试"等测试内容 | 服务器执行 `bash wipe-posts.sh`（自带备份+校验） |
| 2 | 🟡 P0 | 运营 | 生产环境 | 清空后应用首页为**空列表**，审核易被判定"内容/体验不足" | 清空后预置 5–10 条**真实质感内容**（不同体裁/含图/含标签），并准备 2 个可登录演示账号 |
| 3 | 🟡 P1 | 配置 | `build-profile.json5` | 该文件被 `.gitignore`（含签名材料路径），改动只在本地盘，DevEco 可能重写 | **构建发布包前复核**：product 必须选 `release`、signingConfig 必须为 `release`（不是 debug） |
| 4 | 🟢 | 行为确认 | `shareRouter` | `youju.chat/post/1` 返回 404，一度疑似路由未挂载 | 已查证：路由已挂载（app.ts:118），404 因该 id 帖子不存在（正确行为）。**预置内容后分享链路即正常**（本地实测 200 + og:title） |
| 5 | 🟢 | 工具 | `check-agc-identity.mjs` | 依赖受保护的 CI 变量，本地无法运行 | 提审构建在 CI 或本地带变量执行（非阻塞） |

---

## 3. 提审前行动清单

| # | 行动 | 负责方 | 紧急度 | 说明 |
|---|------|--------|--------|------|
| 1 | 服务器清空生产测试数据 | 你（服务器） | **P0** | `cd ~/youju && bash wipe-posts.sh`（会先自动备份，需输入 yes 确认） |
| 2 | 预置生产演示内容 | 你 | **P0** | 用真机 release 包发布 5–10 条不同体裁内容（图文/长文/辩论各 1+，带标签与封面） |
| 3 | 复核签名与 product 后打发布包 | 你（DevEco） | **P0** | 确认 `build-profile.json5` 的 release product 未被动过 → Build App（.app）→ 上传 AGC |
| 4 | 真机 release 包最终冒烟 | 你 | P1 | 用 release 包（连生产）过一遍：登录 → 首页 → 详情 → 点赞/收藏 → 发布 → 我的 → 设置 → 数据导出 |
| 5 | AGC 后台信息核对 | 你 | P1 | 应用信息/分类、隐私政策链接、权限使用说明（相册/相机/网络）、测试账号（如有） |

---

## 4. 待完善 / 未覆盖项（诚实声明）

- **UI 未覆盖**：发布流程的相册选图上传（需人工/真机手动）、评论输入框 UI 输入、系统分享面板（属系统窗口；同一 ShareKit 的数据导出分享面板此前已实测可用）、通知页细节、搜索页 UI
- **未做**：并发/性能压测、弱网与断网异常路径、iOS/其他设备形态适配（项目为 HarmonyOS 单端）
- **生产侧**：所有接口结论基于本地库（生产代码版本是否与本地一致未验证 → 建议部署后跑一次 `BASE=https://api.youju.chat/v1 node scripts/full-chain-check.mjs`，注意会写测试数据）

---

## 5. 本次新增的可复用工具

- `backend/scripts/full-chain-check.mjs` —— 全链路接口自检（13 阶段 87 项断言，逐项 ✅/❌/⚠️ + 汇总 + 非零退出码，可接 CI）
  - 本地：`node scripts/full-chain-check.mjs`
  - 生产：`BASE=https://api.youju.chat/v1 node scripts/full-chain-check.mjs`

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
