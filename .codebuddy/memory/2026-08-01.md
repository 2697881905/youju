# 2026-08-01

## IDE 项目名后缀排查
- 现象：IDE 项目视图显示 `HarmonyProject1[运维与测试手册md]`。
- 根因：DevEco/CodeArts 会抓取项目首个 Markdown 的**标题**（非文件名）作为后缀提示。来源为 `entry/Docs/有据.md` 第 1 行标题 `# 有据 运维与测试手册`。
- 处理：将标题改为 `# 有据 项目文档`，内容保留。改后需刷新/重启 IDE 让缓存更新。
- 应用显示名仍为 `BigBlueBook`（AppScope 配置），包名 `com.bigbluebook.app`，不受影响。
